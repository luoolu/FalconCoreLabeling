## 新增多边形标注共边功能
1，用户已经完成一个多边形A，在新增一个多边形B时，
如果用户绘制多边形B时，多边形B与多边形A有交叉点C和D，CD段则为共边，多边形A的CD段则被多边形B的轮廓线重合；
重合部分精确贴合，端点高亮反馈，可以调整端点位置，调整后，重合部分精确贴合，端点高亮反馈；
2，编辑已标注多边形时，按"shift+拖"多边形B，当B与A交叉时，则交叉部分CD段作为共边，A的CD段则被B的轮廓线重合；
3，共享边高亮与否在view菜单栏里面启用或者关闭，默认不启用；
4，用户画多边形B时，可以与A交叉，但只取CD那段作为共边，其他部分不作为共边；
5，B在A里面被裁剪后，被裁剪的部分没有完全复用A的CD段；需求是被裁剪的部分完全复用A的CD段
6，默认启用共边功能，用户可以在view菜单栏里面启用或者关闭共边功能；
7，被裁剪的B的那段变成共边后，应该精确覆盖A的CD段；
8，默认关闭吸附功能，用户可以编辑菜单栏里面启用或关闭吸附功能；
9,移除共边高亮展示逻辑；
10，多个共边情况处理；✅已完成(2025-10-21)
11，共边方向选择：选择靠近被选中多边形中心的那一侧；✅已完成(2025-10-21)
12，多多边形共边鲁棒性：一次性处理所有交叉点，避免迭代裁剪导致的顶点混乱和直线穿越；✅已完成(2025-10-21)
13，Entry/Exit判断优化：基于边端点而非全局状态，防止同一边多个交叉点误判，识别切线情况；✅已完成(2025-10-21)
14，共边冒尖修复：使用精确交叉点而非近似顶点，智能处理中间顶点，消除突出尖角，提升美观度和精确性；✅已完成(2025-10-22)
15，起始点在内部修复：正确初始化currently_inside状态，处理exit without entry情况，闭合未匹配entry路径，增强鲁棒性；✅已完成(2025-10-22)
16，共边方向选择增强：考虑路径上所有点的平均距离（而非仅中点），特别优化起始点在内部情况，全方位考虑绘制起始点位置，提升鲁棒性；✅已完成(2025-10-22)
17，增加已标注的两个多边形手动指定共边的功能，已有两个标注好的轮廓O1和O2,两者无交叉重叠，启用手动指定两个多边形共边功能，用户绘制多边形，
起始点AG,AG重合，绘制多边形与O1的第一个交叉点为B，与O2的第一个交叉点为C，与O2的第二个交叉点为D，
与O1的第二个交叉点为E，终点为G，则多边形ABCDEG内的O1的BE段被O2的在ABCDEG多边形内的CD段替换以达到人为交互共边的目的；

## 手动共边
  - 手动共边（Lasso）不弹标签框
  - 两个多边形在共边前保持选中[test1.json](../../../../media/luolu/3cc826d6-a968-4df2-9ae7-ee2c324675bd/home/xkjs/data/%E5%85%B1%E8%BE%B9%E6%B5%8B%E8%AF%95%E6%95%B0%E6%8D%AE/test1/test1.json)
  - 去除了多余的小边/短刺
  - 共边后黄色沿红色曲线，直线段不再残留
  - 自动裁剪路径的交点与拼接也已稳健

## 增加画布上指定显示或隐藏某一个标签的功能
1,结合标注统计在画布上只展示或隐藏xx标签；
2,前端界面交互友好；

## 回退功能bug
检查逻辑并已修复

## PPL-XPL标注使用一个json
1,打开文件夹标注，文件夹下有多张图片，用户开启文件夹标签同步，目前是多张图片对应多个json,进行标注，交互修改；
修改为同一个文件夹，如果用户开启文件夹标签同步，则该文件夹下多张图片使用同一个json文件，
json文件名称和文件夹的名称保持一致；


## PPL-XPL标注使用一个json,打开文件夹多张图片多个json
1,打开文件夹，文件夹里面有多张图片，用户开启文件夹标签同步，

## 兼容之前同步标注多个json
为了兼容之前文件夹同步标注多个json，增加弹窗（之前是文件夹下是文件夹名称_S,_0,_15,_30,_45,_60,_75）让用户同意将已有的
标注转移为新格式（和文件夹的名称保持一致的json文件，把之前的标注shapes（选_S的json文件，其他几个标注内容是一样的，
只是名称不一样）转移到新格式的json,然后删除旧json）， 即该文件夹下多张图片使用同一个json文件；
注意之前的标注_S,_0,_15,_30,_45,_60,_75对应的json下标注内容是一样的， 只是文件名称不一样

1, 标注点稀疏程度一致性，确保整张图分割SAM2的轮廓点稀疏程度和自动标注（加减点提示、矩形框提示）点稀疏程度一致（多边形标注）；
2, 整张图分割SAM2等待提示的字体太小，确保常见的笔记本分辨率能适配，且能明显看见等待提示；
3, 标注轮廓多（尤其是多边形，点多），交互卡顿的问题，看看如何优化，确保交互流畅；

## 绘制多边形，共边启用
标注绘制多边形时，如果启用绘制共边（前端顶部菜单栏，放在编辑栏里面，默认启用），
查看我绘制的示意图,用户绘制顺序为A-B-C,忽略用户绘制多边形时的起始点，以闭合多边形的中心为参考方向，
后绘制的多边形与已绘制的多边形共边， 注意多个
